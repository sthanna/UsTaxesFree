# Kubernetes Secrets - TEMPLATE FILE
# DO NOT commit actual secrets to version control
# Use: kubectl create secret generic or external secrets manager

apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: tax-filing-production
type: Opaque
stringData:
  url: postgresql://REPLACE_WITH_ACTUAL_CONNECTION_STRING
  username: REPLACE_WITH_DB_USER
  password: REPLACE_WITH_DB_PASSWORD

---
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: tax-filing-production
type: Opaque
stringData:
  jwt-secret: REPLACE_WITH_ACTUAL_JWT_SECRET
  irs-efile-tcc: REPLACE_WITH_IRS_TCC
  irs-efile-efin: REPLACE_WITH_IRS_EFIN

---
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: tax-filing-production
type: Opaque
stringData:
  url: redis://REPLACE_WITH_REDIS_URL

---
# Instructions for creating secrets securely:
#
# 1. Using kubectl:
#    kubectl create secret generic db-secret \
#      --from-literal=url='postgresql://user:pass@host:5432/db' \
#      --namespace=tax-filing-production
#
# 2. Using AWS Secrets Manager:
#    Install External Secrets Operator
#    kubectl apply -f external-secrets.yaml
#
# 3. Using HashiCorp Vault:
#    Install Vault Secrets Operator
#    Configure Vault integration
